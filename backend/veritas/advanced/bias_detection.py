"""Bias Detection Engine (stub).

Planned:
  Detect ideological bias in source selection and in the synthesized
  explanation itself. Uses Ad Fontes / MBFC signals on sources plus an
  LLM classifier on wording patterns.

Spec reference: user brief, PART 2 Feature 9.
"""


def analyze_bias(sources: list, synthesis: str) -> dict:
    raise NotImplementedError("Bias detection engine — scheduled for PR #4")
