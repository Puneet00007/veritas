"""Socratic Debate Mode (stub).

Planned:
  After a verdict is returned, the user can challenge it. The system:
    1. Evaluates the user's argument for validity / logical fallacies.
    2. If the argument introduces new evidence, re-runs a focused
       research loop constrained to that evidence.
    3. If the argument is valid but does not overturn the verdict, the
       system defends and cites.
    4. If the argument is fallacious, the system names the fallacy and
       explains. All exchanges are logged to the trace.

Spec reference: user brief, PART 2 Feature 1.
"""


class SocraticDebateEngine:
    def __init__(self) -> None:
        raise NotImplementedError("Socratic debate mode — scheduled for PR #3")
